# Shell scripts + systemd units are consumed on the Linux VPS: force LF so a
# Windows checkout (autocrlf) can never commit CRLF that would break the shebang.
*.sh text eol=lf
*.service text eol=lf
*.timer text eol=lf
*.conf text eol=lf
